# ── Local development extras ─────────────────────────────────────────────────
# Install on top of requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt
#
# NOT included in the Docker image.

# Core ML (CPU-only for local dev; swap for a CUDA wheel if you have a GPU)
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0
torchvision

# Dashboard
streamlit
plotly

# AWS CLI (handy locally; boto3 already covers runtime S3 access)
awscli

# Dev / testing
pytest
kaggle
