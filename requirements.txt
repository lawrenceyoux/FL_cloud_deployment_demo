# ── Production deps (used by Docker image) ───────────────────────────────────
# torch is installed separately in the Dockerfile with the CPU-only index so
# that the NVIDIA CUDA wheels (~2.5 GB) are never pulled in.
# For local dev install requirements-dev.txt on top of this file.

# Federated Learning
flwr>=1.5

# Data
pandas
scikit-learn
numpy
pyarrow       # for parquet support

# MLflow — skinny client only (no Flask/gunicorn/server UI deps)
# Pin to same version as the server image (kubernetes/deployments/mlflow.yaml)
# ghcr.io/mlflow/mlflow:v2.22.0 — must stay in sync to avoid API mismatches.
mlflow-skinny==2.22.0

# AWS — SDK only; awscli not needed at runtime
boto3

# Privacy
opacus

# Utilities
python-dotenv
joblib
tqdm
pyyaml
